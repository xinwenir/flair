### Introduction

The bodies of `TET`, `ARB`, `PXY`, `PYY` and `PYZ` are contained in this branch.

### How to run?

* Add the files `Fonts` and `geoviewer.so` to the path `flair-2.3`;
* Running, using: 
```
cd ~/flair-dev-PY/flair2.3
python flair.py
```
